# 电子发票识别工具 - 通用版

## 适用场景
Win10/Win11 电脑，有网络，首次需装环境，之后双击即用。

## 文件清单
```
通用版/
├── invoice_tool.bat        ← 日常双击启动
├── setup.cmd               ← 首次安装环境（只需一次）
├── setup.bat               ← 备用安装脚本
├── 修复OCR缓存.bat          ← OCR 出问题时一键修复
├── requirements.txt        ← 依赖清单
├── app.py                  ← 后端
├── invoice_extractor.py    ← 核心识别逻辑
├── templates/              ← 网页界面
├── excel/                  ← 输出目录
└── 使用说明.md              ← 本文件
```

---

## 首次使用

1. 双击 **setup.cmd**
2. 等待安装完成（自动装 Python + 依赖，约 10-20 分钟）
3. 看到 "Setup complete!" 即完成

> 如果双击 setup.cmd 跳出的是记事本：右键 →「打开方式」→ 选 cmd.exe

---

## 日常使用

1. 双击 **invoice_tool.bat**，浏览器自动打开
2. 点击 **上传 PDF 文件** 或 **上传 PDF 文件夹**，选择文件后点「开始识别」
3. 等待识别完成（进度条走满）
4. 点击 **下载 Excel**，得到识别结果

> 也可在命令行运行 `python app.py` 启动（效果相同）

### 上传方式

| 方式 | 操作 |
|------|------|
| 上传 PDF 文件 | 点击左侧卡片，多选 PDF 文件 |
| 上传 PDF 文件夹 | 点击右侧卡片，选择含 PDF 的文件夹（保留子目录，含多层嵌套） |

### Excel 包含三个工作表

| 工作表 | 内容 |
|--------|------|
| 发票明细 | 每张发票的全部字段（可筛选、排序、求和） |
| 报销汇总 | 按人名汇总金额和发票张数 |
| 报销文件名 | 自动生成报销用文件名（人名+计费时段+类型+金额） |

---

## 支持的发票类型

| PDF 类型 | 识别方式 | 速度 |
|----------|----------|------|
| 增值税电子发票（文本型） | pdfplumber | 秒级，精准 |
| 运营商话费发票（联通/移动/电信） | pdfplumber + 智能排版 | 秒级，无需 OCR |
| 财政/医疗门诊收费票据（图片型） | PaddleOCR | 每页约 2-5 秒 |
| 其他扫描件/图片型 | PaddleOCR | 首次需下载模型约 500MB，之后秒级 |

### 可提取的字段

| 字段 | 增值税电子发票 | 医疗门诊票据 |
|------|---------------|-------------|
| 号码 | 发票号码 | 票据号码 |
| 日期 | 开票日期 | 开票日期 / 就诊日期 |
| 购买方 | 购买方名称 | 交款人 |
| 销售方 | 销售方名称 | 收款单位 |
| 金额 | 价税合计 | 金额合计（小写） |
| 话费专用 | 业务号码、账期 | — |

### 智能过滤（v2.1 新增）

PDF 中混有报销单、明细表、银行卡明细表等非发票表单时，程序会**自动识别并跳过**，只提取其中的电子发票或财政票据。

### v2.4 识别增强（2026-07-07）

- 修复联通等运营商发票：发票号码在「发票号码」标签上方、同一行两个「名称」等排版问题
- 新增浙江省医疗门诊收费票据（体检费等图片型 PDF）全页识别
- OCR 容错：交款人、开票日期、收款单位等字段误识别自动修正
- 话费发票自动提取业务号码和账期

---

## 系统要求

| 项目 | 最低要求 |
|------|----------|
| 系统 | Windows 10 64-bit |
| 内存 | 4 GB |
| 硬盘 | 3 GB 可用空间 |
| 网络 | 首次安装需要 |

---

## 常见问题

**Q: setup.cmd 安装失败？**
通常是网络问题。可以手动安装：
1. 从 https://www.python.org/downloads/ 下载 Python 3.10+
2. 安装时 **勾选 "Add Python to PATH"**
3. 重新双击 setup.cmd

**Q: 双击 invoice_tool.bat 没反应？**
按住 Shift + 右键文件夹空白处 →「在此处打开 PowerShell」→ 输入 `python app.py` 查看报错信息。

**Q: 浏览器没自动打开？**
手动打开浏览器，访问 http://127.0.0.1:5000

**Q: 上传后一直卡在"识别中"？**
首次识别图片型 PDF 需要下载 PaddleOCR 模型（约 500MB），需要 2-5 分钟。后续识别秒级完成。

**Q: PaddleOCR 初始化失败，日志显示 "parse error" 或 "parse an empty input"？**
模型缓存损坏。解决方法：
1. 双击运行「修复OCR缓存.bat」（会同时清理中文用户名下的重定向缓存目录）
2. 重新上传 PDF 即可（无需重启程序）

**Q: PaddleOCR 初始化失败，日志显示 "strides is not right" 或 "InvalidArgument"？**
PaddlePaddle 版本太旧。打开 cmd 运行：
```
pip install paddlepaddle==3.1.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**Q: 下载的模型在哪个盘？**
- 正常情况下模型缓存路径自动显示在 cmd 窗口日志中
- 日志会写：`模型缓存重定向到: X:\paddle_ocr_cache`
- 文件资源管理器直接输入该路径即可查看

**Q: 用户名是中文，会影响识别吗？**
程序会自动检测中文用户名，将模型缓存重定向到纯英文路径（优先 D 盘），无需手动操作。

**Q:电脑识别不了？**
常见原因与处理：
1. **PaddleOCR 未成功加载**（最常见）→ 让对方运行 setup.cmd，或执行 `pip install paddlepaddle==3.1.0 paddleocr`
2. **OCR 模型缓存损坏** → 运行「修复OCR缓存.bat」后重新上传
3. **首次识别图片型 PDF** → 需联网下载约 500MB 模型，等待 2-5 分钟
4. **电脑内存不足** → 关闭其他程序，建议 4GB 以上可用内存
5. 升级到 v2.3 后程序会自动：逐页 OCR、适配 CPU、优化图片尺寸，跨电脑兼容性更好

**Q: 有些 PDF 识别不出来？**
确认 PDF 是正规电子发票格式。如果是拍照、截图等非标准 PDF，识别率会下降。

**Q: 识别结果有误？**
可在 Excel 中手动修正。常见问题：购买方/销售方名称偶尔互换，手动对调即可。

**Q: Excel 打开乱码？**
用 Microsoft Excel 打开，不要用 WPS。WPS 对 UTF-8 编码支持不完善。

**Q: PDF 里混了报销单、明细表，能只识别发票吗？**
✅ v2.1 起自动过滤。程序会逐页判断是否为电子发票，报销单、明细表等非发票页面自动跳过，只提取电子发票信息。

**Q: 如何升级到最新版？**

**从 v2.3 升级到 v2.4**（推荐，无需重装环境）：

1. 关闭正在运行的程序
2. 从 GitHub 下载最新版：https://github.com/boyuan623-coder/invoice-tool
3. 用新版文件覆盖旧文件夹（**至少替换 `invoice_extractor.py`**，建议全部覆盖）
4. 双击 `invoice_tool.bat` 重新启动

> `excel/`、`uploads/`、OCR 模型缓存均无需删除，升级后直接可用。v2.4 **无需**额外 pip install。

**从 v2.0 / v2.1 升级到 v2.4**（首次从旧 exe 版升级）：

1. **关闭正在运行的程序**
2. **用新版文件覆盖旧文件夹中的同名文件**（建议全部替换）：
   - `app.py`、`invoice_extractor.py`、`templates/index.html`
   - `invoice_tool.bat`、`修复OCR缓存.bat`
   - `setup.cmd`、`setup.bat`（可选）
3. **安装新增依赖**（旧版可能没有 waitress）：
   ```
   pip install waitress -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```
4. **改用新启动方式**：双击 `invoice_tool.bat` 或运行 `python app.py`

**从 v2.0 / v2.1 升级后有什么变化？**

| 项目 | 旧版 | 新版 v2.2 |
|------|------|-----------|
| 启动 | 双击 `invoice_tool.exe` | 双击 `invoice_tool.bat` 或 `python app.py` |
| OCR 出错后 | 需关闭程序重开 | 运行「修复OCR缓存.bat」后直接重新上传 |
| 中文用户名 OCR 缓存修复 | 可能清不干净 | 修复脚本会一并清理 `D:\paddle_ocr_cache` 等目录 |
| 选文件夹上传 | 显示全部文件数 | 只统计 PDF 数量 |

**如果暂时无法升级，旧版仍这样用：**

1. 继续用原来的 `invoice_tool.exe` 或 `python app.py` 启动
2. OCR 失败时：手动删除 `%USERPROFILE%\.paddlex`；中文用户名还需删除 `D:\paddle_ocr_cache\.paddlex`（或 E/F/C 盘同名路径）
3. **必须关闭程序后重新打开**，OCR 才会重试（旧版不支持热恢复）
4. 报销汇总里同姓不同名的人，发票张数可能统计偏多，以 Excel 明细表为准

**Q: Win7 能用吗？**
不支持。Python 3.12 要求 Win10+。

---

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v2.4 | 2026-07-07 | 联通发票排版修复、财政/医疗门诊票据支持、OCR 字段容错增强、业务号码/账期提取 |
| v2.3 | 2026-06-28 | 跨电脑兼容：逐页混合 OCR、CPU/GPU 自动适配、OCR 结果格式兼容、图片尺寸优化、发票号码 OCR 容错 |
| v2.2 | 2026-06-27 | 稳定性修复：waitress 生产服务器、OCR 失败后自动重试、报销汇总统计修正、XSS 防护、任务内存管理、启动脚本 invoice_tool.bat |
| v2.1 | 2026-06-26 | 新增智能页面过滤：自动跳过报销单/明细表等非发票表单，只识别电子发票 |
| v2.0 | 2026-06-25 | 初始发布：支持文本型/图片型 PDF，多文件夹批量识别，网页版+命令行版 |
